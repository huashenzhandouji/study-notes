1.明确目的：
    目标，工具；
    重点：电脑，服务器，数据库，程序
2.信息收集：
    方式：主动扫描，开放探索；
    类型：基本信息：ip，网段，域名，端口，操作系统，中间件，数据库，CMS源码，脚本语言；
3.漏洞探测：
（1）借用漏洞库；
（2）根据不同漏洞自己挖掘：弱口令，sql注入，缓冲区溢出，拒绝服务，
     分布式拒绝服务，跨站请求伪造，服务请求伪造，xss攻击；文件包含漏洞
4.漏洞验证，自动化验证，手工验证；
5.信息分析：绕过防御机制（WAF，防火墙）
           定制攻击路径（内网，间接，直接）
           绕过检测机制（代码检查）；
           攻击代码；

          
