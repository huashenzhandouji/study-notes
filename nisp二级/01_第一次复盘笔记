CIA三元组：机密性，完整性，可用性

威胁情报；态势感知
基于时间的PDR PPDR模型：
       PDR:保护、检测、响应PT>DT+RT认为是安全的；
       PPDR：引入ET,ET=(DT+RT)-PT,ET越小说明越安全

IATF信息安全保障技术框架：三保护一支撑
       保护：计算环境保护，区域边界保护，网络基础设施保护；
       支撑：支撑性基础设施；

四大安全：物理安全、网络安全、系统安全、应用安全

OSI参考模型：应用，表示，会话，传输，网络，数据链路，物理；
物理层：bit，同轴电缆，光纤，双绞线，集线器，中继器；
数据链路层：帧，交换机，网桥，网卡；LLC MAC两层(逻辑链路控制 媒体访问控制)；PPP ARP RARP IEEE802.2  IEEE802.3（检错但不纠错）
网络层:包，三层交换机，路由器;IP;
传输层：报文；端口；TCP UDP;(数据检错恢复）
会话层：会话的建立，联系，断开；
表示层：协议转换，字符转换，数据的加解密，数据的压缩；
应用层：接口，HTTP,HTTPS;

OSI安全架构：五大安全服务；八大安全机制；
安全服务：鉴别，访问控制，数据完整性，数据机密性，抗抵赖性；
安全机制：加密，数字签名，访问控制，数据完整性，鉴别交换，业务流填充，路由控制，公证

tcp三次握手四次挥手：
三次握手：
客户端给服务器发送请求连接（SYN=1 seq=j)
服务器收到后同意连接发送（SYN=1, ACK=1, seq=k, ack=j+1）报文
客户端收到报文后给服务器发送（ ACK=1, seq=j+1, ack=k+1）报文，同意连接
四次挥手：
客户端请求断开连接发送（FIN=1，Seq=U(最后一个数据报文+1））随后进入等待断开1状态
服务器收到后发送（ACK=1, ack=u+1, seq=v)进入最后发送数据阶段；
客户端收到服务器的报文后，进入等待断开2状态，（等待服务器发送连续释放报文）
服务器发送完数据后给客户端发送连续释放报文（FIN=1, ACK=1, ack=u+1, seq=w），进入等待断开状态；
客户端收到报文后，给服务器返回（ACK=1, ack=w+1, seq=u+1），等待2msl（最长报文寿命）后客户端收到报文，最后关闭


