一、漏洞等级划分
1. 高危漏洞（远程 RCE、控服务器、拖数据库、入侵内网，优先级最高）：
   SQL 注入、任意文件上传、本地 / 远程文件包含、远程代码执行 RCE、远程命令执行 CMD、PHP/Java/.NET/Python 反序列化、
   目录穿越（路径遍历）、任意文件下载、中间件解析漏洞、框架漏洞（Fastjson、Shiro、ThinkPHP、Spring 漏洞）、
   后台未授权访问 RCE、SSRF（可内网探测 + 端口扫描 + 读取本地文件）
2. 中危漏洞（不能拿服务器，但窃用户数据、篡改业务、非法获利，SRC 核心）：
   水平越权、垂直越权、接口未授权访问、验证码绕过 / 复用 / 爆破、密码重置逻辑缺陷、支付篡改、订单篡改、优惠券滥用、短信逻辑漏洞、
   批量遍历用户信息、存储 XSS、DOM 型 XSS、CSRF 跨站请求伪造、会话固定、接口参数篡改
3. 低危漏洞（仅辅助渗透，无直接数据窃取与服务器控制）：
   反射型 XSS、目录列举、前台弱口令、多余测试页面、前端敏感明文泄露、多余开放端口、错误报错泄露路径。
4. 信息泄露漏洞（附属漏洞，配合高危漏洞利用）：
   SVN/GIT 源码泄露、备份压缩包泄露、数据库配置文件泄露、phpinfo 探针泄露、日志文件泄露、网站物理路径泄露、数据库备份文件泄露

二、各场景漏洞重点内容：
1.CTF：偏重漏洞原理、各种畸形绕过、伪协议组合、POP 链构造，以 SQL、上传、包含、反序列化、代码执行为主，逻辑漏洞很少；
2.SRC：90% 为业务逻辑漏洞（越权、验证码、支付），高危 RCE 漏洞稀缺，少量 XSS、信息泄露；
3.红蓝对抗：外网靠 SQL、上传、包含、命令执行打点拿权限，内网依靠 SSRF、中间件漏洞横向渗透；
4.实战渗透（等保 / 企业挖洞）：老旧 CMS 集中爆发 SQL、上传、反序列化、代码执行；新开发项目大多只有逻辑漏洞。

三、各类漏洞危害情况
（一）高危漏洞明细
1.SQL 注入
   原理：用户可控参数直接拼接 SQL 语句，无过滤，可控语句被数据库执行。
   细分：联合注入、报错注入、布尔盲注、时间盲注、堆叠注入、宽字节注入、二次注入、HTTP 头注入 (Cookie/UA/Referer)、多语句注入。
   危害：脱全库（手机号、账号密码、身份证）、读写服务器文件、执行系统命令、拿下数据库权限后提权入侵内网。
2. 文件上传漏洞
   原理：后端校验后缀 / Content-Type 不严谨，绕过过滤上传脚本文件 (php/jsp/aspx)，中间件解析执行木马。
   绕过方式：黑名单绕过、MIME 类型伪造、后缀双写、%00 截断、图片马 + 文件包含解析漏洞、Nginx/IIS/Apache 畸形解析漏洞、空格 + 点绕过。
   危害：上传大马、篡改网站源码、窃取服务器文件、服务器提权、内网横向渗透。
3. 文件包含漏洞
   原理：include/require等函数引入用户可控文件，无过滤，使用伪协议读取源码或执行图片马。
   常用伪协议：php://input、php://filter、file://、zip://、phar://。
   细分：本地文件包含 LFI、远程文件包含 RFI（allow_url_include 开启）。
   危害：读取数据库账号配置、网站源码、系统敏感文件（/etc/passwd），配合上传实现 RCE。
4. 反序列化漏洞
   原理：unserialize/ObjectInputStream 等函数接收可控用户输入，恶意序列化数据被解析，触发魔术方法实现代码执行。
   分类：PHP 反序列化、Java 反序列化 (Fastjson/Shiro/CC 链)、Python pickle 反序列化。
   危害：无文件落地远程命令执行，直接获取服务器权限，批量入侵集群服务器。
5. 代码 / 命令执行漏洞
   原理：eval/assert/system/exec/passthru等危险函数接收外部可控参数，直接执行 PHP 代码或系统 CMD 命令。
   触发点：变量覆盖、可控参数直接带入危险函数、过滤绕过。
   危害：任意操作系统指令、读写文件、创建管理员账号、服务器提权。
6.SSRF 漏洞
   原理：服务器端请求伪造，后端主动访问用户可控 URL，可探测内网、访问本地文件、请求内网业务接口。
   分类：普通 SSRF、DNS 重绑定 SSRF、Filter 绕过 SSRF。
   危害：内网端口扫描、访问内网未授权服务、读取本地敏感文件。
7. 路径遍历（目录穿越）
   原理：../等跳转符未过滤，跨目录读取服务器任意文件（配置、密钥、数据库）。
（二）中危逻辑漏洞（SRC 核心，CTF 极少，红蓝 / 实战零星存在）
1. 越权漏洞
   水平越权：同权限用户越权查看他人订单 / 信息；垂直越权：普通用户越权获取管理员权限、访问后台接口。
   成因：仅前端鉴权，后端无权限校验。
   危害：批量爬取全站用户隐私数据。
2.验证码漏洞：验证码明文返回、验证码复用、前端删除校验直接提交、验证码可爆破。
3.支付逻辑：商品金额前端可控、订单篡改、0 元下单、优惠券无限领用。
4.XSS：存储 XSS 插入后台，窃取管理员 Cookie 登录后台；DOM-XSS 纯前端渲染漏洞。
5.CSRF：伪造链接，诱导管理员点击，修改账号密码、绑定手机号。
（三）低危 + 信息泄露漏洞
   Git/Svn 泄露：网站源码被打包泄露，源码里明文数据库账号密码；
   备份压缩包(.zip/.rar/.bak) 泄露，直接下载全站源码与数据库备份；
   phpinfo()泄露暴露服务器路径、中间件配置。
   作用：单独无危害，为 SQL、上传、代码执行提供关键配置信息。

四、现存漏洞形势问题
CTF与实战脱节：CTF多构造畸形特殊环境漏洞，实战多是 CMS / 框架原生漏洞，部分 CTF 技巧在真实站点无法复用；
SRC漏洞同质化严重：大量提交重复越权、验证码漏洞，高危 RCE 类漏洞逐年变少，业务逻辑成为 SRC 挖洞主流；
红蓝对抗边界防护变强：WAF、云防火墙拦截常规 SQL / 上传，传统 web 漏洞打点难度提升，转向 0day、中间件漏洞、内网漏洞；
实战建站漏洞两极分化：老旧开源 CMS 漏洞泛滥（上传、注入遍地），自研框架防护完善、高危漏洞稀缺，挖洞重心转向业务逻辑；
防护侧短板：多数企业重视 WAF 防护代码漏洞，但忽视业务逻辑校验，导致逻辑漏洞持续高发。
