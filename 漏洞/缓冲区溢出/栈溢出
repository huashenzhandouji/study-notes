栈溢出：
  堆栈在物理上指的是一组连续分配的内存空间；（后进先出，ESP,EBP,EIP）
  1.原理：
   写入超过预留长度的数据，造成缓存的溢出从而破坏程序的堆栈，导致执行远程改变；
    寻找程序的漏洞->编制缓冲区溢出的程序->精确控制跳出地址->执行设定的代码->获得系统权限破环系统；
  2.危害：
   最大数量的漏洞，漏洞危害高，
  3.防范：
   用户：安全补丁，防火墙：
   开发人员：编写安全代码，对输入的数据进行验证，使用相对安全的函数：
   系统：缓冲区不可执行的技术，虚拟化技术：
  
